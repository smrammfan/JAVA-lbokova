There are 2 packages: task1 and task2.
Each task can be launched by running Main class in appropreate package.

Each package has its own Main class.

Each main method contains two options to run application in two different ways. First option is enabled by default,
and second one is commented. To run second option, please uncomment it and comment the first option.
